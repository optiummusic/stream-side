[package]
name = "common"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
postcard = { version = "1.1.3", features = ["use-std"] }
serde = { version = "1.0.228", features = ["derive"] }
bytes = "1.11.1"